# Public hostname for HTTPS.
# Temporary free-domain option: <droplet-ip>.sslip.io
DOMAIN=203.0.113.10.sslip.io

# Email used for Let's Encrypt registration and expiry notices.
LETSENCRYPT_EMAIL=ops@example.com

# HTTP Basic Auth for all routes.
BASIC_AUTH_USER=demo
# Generate with:
# docker run --rm caddy:2.10-alpine caddy hash-password --plaintext 'change-me'
# Note: in docker compose env files, literal '$' must be written as '$$'.
BASIC_AUTH_HASH=$$2a$$14$$replace_with_bcrypt_hash
